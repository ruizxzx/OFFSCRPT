import React, { useCallback, useEffect, useState } from 'react';
import { RefreshCw, ShieldCheck } from 'lucide-react';
import { adminApprovePayout, adminListPayouts, adminReconcilePayout, adminRejectPayout, type CreatorPayout } from '../lib/commerce';
import { notifyToast } from '../lib/toast';
const inr=(paise:number)=>`₹${(Number(paise||0)/100).toLocaleString('en-IN',{minimumFractionDigits:2,maximumFractionDigits:2})}`;
export const PayoutAdminPanel:React.FC=()=>{const[payouts,setPayouts]=useState<CreatorPayout[]>([]);const[loading,setLoading]=useState(true);const[busy,setBusy]=useState('');const load=useCallback(async()=>{setLoading(true);try{const r=await adminListPayouts();setPayouts(r.payouts||[]);}catch(e:any){notifyToast(e?.message||'Payouts unavailable.','error');}finally{setLoading(false);}},[]);useEffect(()=>{void load();},[load]);const run=async(id:string,action:'approve'|'reject'|'reconcile')=>{setBusy(`${action}:${id}`);try{if(action==='approve')await adminApprovePayout(id);else if(action==='reject')await adminRejectPayout(id,'Rejected by admin.');else await adminReconcilePayout(id);notifyToast(`Payout ${action} complete.`,'success');await load();}catch(e:any){notifyToast(e?.message||`Payout ${action} failed.`,'error');}finally{setBusy('');}};return <section className="border-2 border-black bg-white p-5 space-y-3"><div className="flex items-center justify-between gap-3"><div><div className="font-mono text-[10px] font-black uppercase">V94 · PAYOUT OPERATIONS</div><h3 className="font-display font-black text-2xl uppercase">Vendor Payouts</h3></div><button onClick={()=>void load()} className="border-2 border-black px-3 py-2 font-mono text-[9px] font-black"><RefreshCw className="inline w-3 h-3 mr-1"/>{loading?'LOADING':'REFRESH'}</button></div><div className="border-2 border-black bg-neutral-100 p-3 font-mono text-[8px] flex gap-2"><ShieldCheck className="w-4 h-4"/><span>Financial actions are server-authorized and audit logged. Route transfer outcomes are reconciled before completion.</span></div>{payouts.length===0?<div className="font-mono text-[9px] text-neutral-500">NO PAYOUT REQUESTS.</div>:<div className="space-y-2">{payouts.map(p=><div key={p.id} className="border-2 border-black p-3 grid lg:grid-cols-[1fr_auto_auto] gap-3 items-center"><div><div className="font-mono text-[8px] text-neutral-500">{p.creatorId} · {p.payoutId}</div><div className="font-display font-black text-xl">{inr(p.amountPaise)}</div><div className="font-mono text-[8px] uppercase">{p.status} · {p.providerTransferStatus||'NO PROVIDER STATE'}</div></div><div className="font-mono text-[8px] text-neutral-500">{p.razorpayTransferId||'NO TRANSFER'}</div><div className="flex flex-wrap gap-1 justify-end">{['requested','pending_review'].includes(p.status)&&<><button disabled={busy===`approve:${p.payoutId}`} onClick={()=>void run(p.payoutId,'approve')} className="border-2 border-black bg-[var(--color-primary)] px-2 py-1 font-mono text-[8px] font-black">APPROVE</button><button disabled={busy===`reject:${p.payoutId}`} onClick={()=>void run(p.payoutId,'reject')} className="border-2 border-black px-2 py-1 font-mono text-[8px] font-black">REJECT</button></>}{['processing','reconciliation_required'].includes(p.status)&&<button disabled={busy===`reconcile:${p.payoutId}`} onClick={()=>void run(p.payoutId,'reconcile')} className="border-2 border-black bg-white px-2 py-1 font-mono text-[8px] font-black">RECONCILE</button>}</div></div>)}</div>}</section>;};
