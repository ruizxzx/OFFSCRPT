# OFFSCRPT V88.0.2 — Vercel Deployment Hardening

## Fixes
- Fixed the server-side R2 presigned URL expiration reference.
- Fixed the OFFSCRPT retrieval intelligence-config typing so all configured intelligence flags type-check correctly.
- Removed the old `/api/lib` server helper architecture from the V88 package; shared server code belongs under `/server` so Vercel Hobby does not count helpers as Serverless Functions.
- Kept the canonical API surface at 9 route files.
- Synced package and lockfile versions to 0.88.2.

## Vercel stale-file protection
A `.vercelignore` rule excludes `api/lib/**` so an older Git checkout cannot turn legacy shared helpers into extra Serverless Functions. The repository should still delete the stale directory permanently.

## Critical repository cleanup
The previous V88.0.1 GitHub deployment log referenced `api/lib/digital-products-server.ts` and `api/lib/offscript-retrieval.ts`. Those are stale paths from the pre-V88 layout. They must be deleted from the Git repository, not merely overwritten by the ZIP, because untracked/obsolete files remain when extracting over an existing checkout.

Expected API route count: 9.
